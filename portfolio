<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Coding Portfolio</title>
    <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&display=swap" rel="stylesheet">
    <style>
        /* GENERAL STYLES */
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Indie Flower', cursive;
            background-color: #f9f9f9;
            color: #333;
            line-height: 1.6;
        }

        a { text-decoration: none; color: inherit; }

        /* HEADER */
        header {
            background-color: #4CAF50;
            color: #fff;
            text-align: center;
            padding: 3rem 1rem;
        }
        header h1 { font-size: 3rem; margin-bottom: 0.5rem; }
        header p { font-size: 1.2rem; }

        /* SECTIONS */
        section {
            max-width: 900px;
            margin: 2rem auto;
            padding: 0 1rem;
        }
        h2 {
            color: #4CAF50;
            font-size: 2rem;
            margin-bottom: 1rem;
            text-align: center;
        }

        /* ABOUT & SKILLS */
        #about p { text-align: center; font-size: 1.1rem; }
        #skills ul {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            list-style: none;
            margin-top: 1rem;
        }
        #skills li {
            background-color: #fff;
            padding: 0.8rem 1.2rem;
            margin: 0.5rem;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        #skills li:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* PROJECTS */
        .projects {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
            margin-top: 1rem;
        }
        .project-card {
            background-color: #fff;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 3px 7px rgba(0,0,0,0.1);
            transition: transform 0.2s, box-shadow 0.2s;
            text-align: center;
        }
        .project-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }
        .project-card a {
            font-weight: bold;
            color: #4CAF50;
        }

        /* CONTACT */
        #contact p {
            text-align: center;
            margin: 0.5rem 0;
        }

        /* FOOTER */
        footer {
            background-color: #4CAF50;
            color: #fff;
            text-align: center;
            padding: 1rem;
            margin-top: 2rem;
        }

        /* RESPONSIVE */
        @media(max-width: 600px){
            header h1 { font-size: 2rem; }
            h2 { font-size: 1.5rem; }
        }
    </style>
</head>
<body>

    <header>
        <h1>Your Name</h1>
        <p>Coder | Handwriting Artist | Tech Enthusiast</p>
    </header>

    <section id="about">
        <h2>About Me</h2>
        <p>Hello! I'm a passionate coder skilled in C and web development. I create clean, professional programs and enjoy turning ideas into projects that work perfectly.</p>
    </section>

    <section id="skills">
        <h2>Skills</h2>
        <ul>
            <li>C Programming</li>
            <li>HTML & CSS</li>
            <li>JavaScript Basics</li>
            <li>Project Management</li>
        </ul>
    </section>

    <section id="projects">
        <h2>Projects</h2>
        <div class="projects">
            <div class="project-card">
                <a href="https://github.com/YourUsername/SmartCalculator" target="_blank">Smart Calculator (C)</a>
                <p>Advanced menu-driven C program with math functions and error handling.</p>
            </div>
            <!-- Add more project cards here -->
        </div>
    </section>

    <section id="contact">
        <h2>Contact</h2>
        <p>Email: your.email@example.com</p>
        <p>GitHub: <a href="https://github.com/YourUsername" target="_blank">github.com/YourUsername</a></p>
    </section>

    <footer>
        &copy; 2025 Your Name
    </footer>

</body>
</html>
